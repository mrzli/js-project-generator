{
  "name": "<%= scopeName %><% scopeName ? '/' : '' %><%= projectName %>",
  "version": "0.0.1",
  "description": "",
  "author": "<%= author %> (<%= email %>)",
  "license": "MIT",
  "keywords": [],
  "keywords": [
    "prettier",
    "prettier-config"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/<%= githubUserOrOrg %>/<%= projectName %>"
  },
  "main": "dist/index.js",
  "scripts": {
    "start:dev": "ts-node src/index.ts",
    "build": "rm -rf ./dist && tsc"
  },
  "dependencies": {
    <%- dependencies.map(({ name, version }) => `"${name}": "${version}"`).join(',\n    ') %>
  },
  "devDependencies": {
    <%- devDependencies.map(({ name, version }) => `"${name}": "${version}"`).join(',\n    ') %>
  }
}
